# Auto detect text files and perform LF normalization
* text=auto

# Ensure shell scripts always use LF line endings
*.sh text eol=lf

# Python files use LF
*.py text eol=lf

# Service files use LF
*.service text eol=lf

# Windows batch files use CRLF
*.bat text eol=crlf
*.cmd text eol=crlf
